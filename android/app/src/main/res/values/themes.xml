<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Ispcms" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme — replaced by Compose content immediately after -->
    <style name="Theme.Ispcms.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Ispcms</item>
    </style>
</resources>
